import styled from 'styled-components';

const Wrapper = styled.div`
   z-index: 20;
   position: fixed;
   top: 0;
   width: 30%;
   left: 20%;

   > button {
      margin-right: 1em;
   }
`;

export const SColorMode = {
   Wrapper,
};
