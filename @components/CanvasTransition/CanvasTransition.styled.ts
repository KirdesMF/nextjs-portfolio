import styled from 'styled-components';

const Canvas = styled.canvas`
   width: 100%;
   height: 100%;

   position: fixed;
   top: 0;
   left: 0;

   z-index: 2;

   background: black;
`;

export const SCanvasHexagons = {
   Canvas,
};
